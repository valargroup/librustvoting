## What changed

Describe the behavior change and why it is needed.

## Validation

List the tests or manual checks that cover the change.

## Backport

Apply `A:backport/v3.x` only when this change should also ship on the active
`release/v3.x` maintenance line. Otherwise, leave the target label unset. See
[Release branches and backports](docs/release-branches.md).
